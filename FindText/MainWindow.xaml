<Window x:Class="FindText.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"        
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable = "d"         
        xmlns:app="clr-namespace:FindText"
        xmlns:Converts="clr-namespace:FindText.Converts"
        xmlns:localCtrls="clr-namespace:FindText.LocalControls"
        xmlns:uc="clr-namespace:FindText.UC"
        Height="600" Width="900"
        Icon="/Assets/title.png"
        WindowStyle="SingleBorderWindow"    
        BorderBrush="{DynamicResource BorderBrush}"        
        x:Name="mainWnd"
        Title="{Binding [Main.Title],Source={x:Static app:TextCache.Text}}" 
        
        >

    <Window.Resources>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Brush04}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

    </Window.Resources>

    <Grid Background="{DynamicResource BackgroundBrush}"  >
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="90*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="DivHeader"  Visibility="Collapsed"  VerticalAlignment="Top"  Height="32" Grid.ColumnSpan="2" Panel.ZIndex="9"  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>

            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" Panel.ZIndex="99" >

                <Button x:Name="btnMin" Style="{DynamicResource  MinButtonStyle}" Grid.Column="4" Width="32" />
                <ToggleButton x:Name="btnMax" Style="{DynamicResource MaxButtonStyle}" Width="32" />
                <Button x:Name="btnClose" Style="{DynamicResource CloseButtonStyle}" Grid.Column="4" Width="32" Visibility="Visible" />

            </StackPanel>

        </Grid>

        <Grid x:Name="DivContent" Margin="6,0,6,0"  Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="90*"/>
            </Grid.RowDefinitions>
        </Grid>

        <uc:UCFindText x:Name="ucFindText1"  Grid.Row="0" Grid.RowSpan="2"  />

        <Grid x:Name="DivMask" Background="{DynamicResource Brush05}" Grid.RowSpan="5" Grid.ColumnSpan="5"  Opacity="0.01" Visibility="Collapsed"  >
        </Grid>

        <!-- 设置面板 （界面效果用后台代码实现） -->
        <Grid   x:Name="DivSettings" Grid.RowSpan="10" Visibility="Collapsed" d:Visibility="Collapsed" Width="300" Background="{DynamicResource BackgroundBrush2}"  HorizontalAlignment="Right" Margin="0,0,0,0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="90*"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            
            <TextBlock x:Name="textblockVer"  Text="Ver." HorizontalAlignment="Left"  Margin="32,20,0,0" FontSize="13" Foreground="{DynamicResource Brush03}"  />

            <uc:UCSettings  Grid.Row="1" />
        </Grid>


        <Button x:Name="buttonSetting" Width="32" Height="26" HorizontalAlignment="Right" VerticalAlignment="Top" ToolTip="工具" Margin="0,14,0,0" Background="Transparent" >
            <Path Data="M0,20 L35,20 35,23 0,23 z M0,10 L35,10 35,13 0,13 z M0,0 L35,0 35,3 0,3 z" Fill="{DynamicResource HighlightBrush}" Width="14" Height="12" Stretch="Fill" />
        </Button>

        <StackPanel Grid.ColumnSpan="3" Grid.Row="2" Orientation="Horizontal">

            <Button  Content="Dubug" Width="80" Click="Button_Click"/>

        </StackPanel>
    </Grid>

</Window>